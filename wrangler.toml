# AUTO-GENERATED from providers.toml — do not edit directly.
# Regenerate with: ./generate-wrangler.sh

name = "my-gtfs-worker"
main = "worker/build/worker/shim.mjs"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = true

[build]
command = "bash ./build.sh"

# ─── Provider D1 Databases ──────────────────────────────────────────────────
# All providers share the same worker, but queries route to the specific D1 database.
#
# Deploy:     ./deploy.sh
# Logs:       wrangler tail

# ── mybas-johor ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_MYBAS_JOHOR"
database_name = "gtfs-mybas-johor-db"
database_id = "ceab48e4-22b9-41ab-8280-3fdaceb2651e"
migrations_dir = "migrations/mybas-johor"

# ── ktmb ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_KTMB"
database_name = "gtfs-ktmb-db"
database_id = "83d27857-1ed2-49a1-91d8-452a30c3fb3e"
migrations_dir = "migrations/ktmb"

# ── rapid-bus-mrtfeeder ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_RAPID_BUS_MRTFEEDER"
database_name = "gtfs-rapid-bus-mrtfeeder-db"
database_id = "bb1f9bf5-f4b3-43be-8e83-b51b2fb54bd9"
migrations_dir = "migrations/rapid-bus-mrtfeeder"

# ── rapid-rail-kl ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_RAPID_RAIL_KL"
database_name = "gtfs-rapid-rail-kl-db"
database_id = "b8323ceb-d188-439c-a2dd-3c4b89f817b6"
migrations_dir = "migrations/rapid-rail-kl"

# ── rapid-bus-kl ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_RAPID_BUS_KL"
database_name = "gtfs-rapid-bus-kl-db"
database_id = "102d2871-a2c8-4930-999e-9a1b1262d4f6"
migrations_dir = "migrations/rapid-bus-kl"

# ── rapid-bus-penang ─────────────────────────────────────────────────────────────
[[d1_databases]]
binding = "DB_RAPID_BUS_PENANG"
database_name = "gtfs-rapid-bus-penang-db"
database_id = "3b092c45-d136-4c12-bd8e-ee4734d2affb"
migrations_dir = "migrations/rapid-bus-penang"
